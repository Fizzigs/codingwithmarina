/*package UF2.GestionIntDouble;

import java.util.Scanner;

public class GestionIntDouble {

    String pregunta1 = "";


    public static void main(String[] args) {
        private Scanner scanner1 = new Scanner (System.in);

        System.out.println("Es decimal o entero");
        String pregunta1 = scanner1.nextLine();
        (!scanner1.matches("Stop") && !scanner1.matches("stop") && !scanner1.matches("STOP") )

        if (pregunta == "entero"(scanner1.matches("Int") || scanner1.matches("int")){
            calcint jpp2 = new calcint();
            System.out.println(("max, min o mitj?"));
            int max = jpp2.calcularMaxim();
            int min = jpp2.calcularMinim();
            int mitjana = jpp2.calcularMitjana();
        }
        else if (pregunta == "decimal"(scanner1.matches("Int") || scanner1.matches("int")){
            calcdouble jpp = new calcdouble();
            System.out.println(("max, min o mitj?"));
            double max = jpp.calcularMaxim();
            double min = jpp.calcularMinim();
            double mitjana = jpp.calcularMitjana();
        }

        System.out.println("La nota màxima és " + max + ".");
        System.out.println("La nota mínima és " + min + ".");
        System.out.println("La mitjana de les notes és " + mitjana + ".");
    }
}*/         